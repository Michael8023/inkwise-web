# PDF AI 阅读器浏览器插件开发规划

## 1. 目标与技术路线

开发一个面向小范围注册用户的 Chromium 插件 MVP，首版支持：

- 打开本地 PDF 和公开 PDF URL，可选接管浏览器默认 PDF 页面。
- PDF 目录、缩放、翻页、搜索和文本选择。
- 划词翻译、术语解释、段落解析。
- 三行摘要、全文摘要和针对 PDF 的连续问答。
- AI 回答必须提供页码与原文引用。
- 邮箱验证码登录、默认使用额度、对话历史和多模型切换。
- 原始 PDF 保留在本地；云端只保存文档指纹、必要文本分块、向量和对话记录。
- 暂不支持扫描件 OCR、普通网页解析、支付、团队空间和跨设备原文件同步。

建议单人全职开发周期为 6 周，另预留 1～2 周测试和用户反馈修复。

## 2. 复用方案与系统架构

采用 TypeScript Monorepo，避免插件与后端重复维护类型：

```text
apps/
  extension/       WXT + React 浏览器插件
  api/             Fastify API 服务
packages/
  contracts/       API 类型、Zod Schema、SSE 事件
  ai/              模型适配、Prompt、引用校验
  translation/     百度/腾讯/网易等翻译服务适配与术语配置
  document/        文本标准化、分块与检索
```

直接复用以下成熟组件：

- 插件框架：`WXT`，处理 Manifest V3、构建、热更新和 Chrome/Edge 打包。
- PDF 阅读：Mozilla `pdfjs-dist`，不自行实现 PDF 渲染、目录、文本层和搜索。
- UI：React、Tailwind CSS、Radix UI、Lucide Icons。
- 服务状态：TanStack Query；插件本地状态使用 Zustand。
- 登录与数据库：Supabase Auth + PostgreSQL。
- 文档检索：Supabase `pgvector`，配合 PostgreSQL 全文检索实现混合召回。
- AI 接入：Vercel AI SDK 的 provider adapters，统一处理 OpenAI、Claude、Gemini 及兼容接口。
- 翻译接入：服务端适配百度翻译、腾讯机器翻译、网易有道等专用翻译 API；不使用大模型完成常规划词翻译。
- 文本分块：`@langchain/textsplitters`，不引入完整 LangChain Agent 框架。
- 流式响应：SSE。
- 输入输出验证：Zod。
- 错误监控：Sentry；结构化日志使用 Pino。
- 测试：Vitest、Playwright、MSW。

首版不引入自建向量数据库、消息队列、微服务、Agent 框架、OCR 或 GROBID。耗时任务先采用数据库任务状态与受限并发处理，确有吞吐瓶颈后再引入队列。

## 3. 分阶段实现

### 第一阶段：工程基础与 PDF 阅读器，第 1～2 周

- 创建 pnpm workspace，统一 TypeScript、ESLint、测试和环境配置。
- 构建 WXT 插件壳，包括 PDF Viewer 页面、右侧 AI 面板、设置页和登录页。
- 基于 PDF.js 实现本地文件选择、拖放打开和 URL 打开。
- URL PDF 由扩展 Service Worker 下载，处理跨域、下载失败和文件类型校验。
- 实现页码、缩放、目录、文本搜索、虚拟化页面渲染和阅读位置恢复。
- 在 PDF.js text layer 上实现稳定选区，保存页码、选中文本和矩形坐标。
- 增加“是否接管 PDF 链接”设置，默认关闭，避免改变用户现有浏览行为。

### 第二阶段：文档解析与检索，第 3 周

- 在插件侧通过 PDF.js 按页提取文本，原始 PDF 不上传。
- 使用文件哈希、页数和文本摘要生成稳定文档指纹，避免重复解析。
- 后端进行文本清洗和递归分块；每块保留文档 ID、页码范围、字符范围和原文。
- 为分块生成 embedding，并写入 PostgreSQL/pgvector。
- 查询时执行关键词与向量混合召回，再使用轻量 rerank 选取上下文。
- 对双栏论文采用 PDF.js 当前阅读顺序；检测到明显乱码或文本缺失时明确提示“不支持扫描件或复杂版式”，不生成低可信回答。
- 文档处理状态统一为 `local → extracting → indexing → ready/failed`，界面展示可恢复进度。

### 第三阶段：翻译、摘要与 AI 问答，第 4～5 周

- 划词浮层提供翻译、解释、深入解析和添加到提问四个操作。
- 默认翻译为简体中文，设置中允许更改目标语言。
- 短文本翻译直接调用服务端配置的专用翻译 API，不进入向量检索，也不消耗大模型额度。
- 术语解释、深入解析、摘要和问答才调用 AI 云服务；解释请求附带选区及必要上下文。
- 全文摘要采用分块摘要后合并的 Map-Reduce 流程，并缓存最终结果。
- AI 问答通过 SSE 返回增量文本、引用和完成状态。
- 每条引用包含页码、原文片段和分块 ID；点击引用跳转并高亮对应页面。
- 模型输出无法匹配到检索原文时，不展示伪造引用，并标记“未能从文档中验证”。
- 对话上下文只保留必要轮次摘要，不在每次请求中重复发送整篇论文。
- 大模型列表由服务端配置，前端只展示当前可用模型；服务端按解释、摘要和问答任务映射模型。翻译服务由后端路由，不暴露供应商选择和密钥。
- 为超时、限流、模型不可用和流中断提供重试及切换模型入口。

### 第四阶段：账户、额度与 Beta 交付，第 6 周

- 使用 Supabase 邮箱验证码登录，插件安全存储短期会话，不保存模型供应商密钥。
- 建立按用户统计的请求次数、输入/输出 token、embedding 用量和每日限额。
- 服务端执行认证、限流、文档所有权检查、请求大小限制和供应商密钥隔离。
- 提供历史文档和对话列表；删除文档时级联删除文本块、向量、摘要和对话。
- 增加隐私说明、数据删除入口和“本地 PDF 不上传”的明确状态提示。
- 生成 Chrome/Edge 可安装包，先通过私有分发或未公开商店测试。
- 收集解析失败率、首个 AI token 延迟、问答成功率和引用点击率，不记录 PDF 原文到日志。

## 4. 接口与数据契约

核心公共类型：

- `Document`：文档指纹、标题、页数、语言、处理状态和创建时间。
- `PageText`：页码、标准化文本和可选文本块位置信息。
- `DocumentChunk`：分块 ID、文本、页码范围、字符范围和 embedding 状态。
- `SelectionContext`：文档 ID、页码、选中文本、前后文和矩形坐标。
- `TranslationResult`：原文、译文、源语言、目标语言、翻译服务标识和缓存状态。
- `Citation`：分块 ID、页码、原文片段和匹配分数。
- `ChatEvent`：`delta | citation | usage | error | done`。
- `ModelOption`：模型 ID、显示名称、能力、上下文限制和可用状态。

核心 API：

- `POST /v1/documents`：创建或命中已有文档指纹。
- `POST /v1/documents/:id/pages`：批量上传按页提取的文本。
- `POST /v1/documents/:id/index`：完成分块和索引。
- `GET /v1/documents/:id/status`：读取处理状态。
- `POST /v1/translate`：调用专用翻译服务翻译选中文本。
- `POST /v1/explain`：调用 AI 云服务解释选中文本或术语。
- `POST /v1/documents/:id/summary`：生成或读取缓存摘要。
- `POST /v1/documents/:id/chat`：SSE 文档问答。
- `GET /v1/models`：返回用户可用模型。
- `DELETE /v1/documents/:id`：删除用户云端派生数据。

所有接口使用 Supabase JWT，服务端通过共享 Zod Schema 校验；幂等接口使用文档指纹或请求 ID 防止重复扣费和重复索引。

## 5. 测试与验收

- 使用公开、无版权限制的单栏、双栏、中英文和超长论文 PDF 作为固定测试集。
- 验证本地文件、公开 URL、重定向 URL、无效 PDF、加密 PDF 和跨域失败场景。
- 验证缩放、翻页后选区坐标、高亮恢复、引用跳转和刷新后的阅读位置。
- 验证相同 PDF 不重复生成 embedding，索引失败后可以恢复。
- 验证摘要覆盖主要章节，问答引用页码与原文实际一致。
- 验证断网、模型超时、429、SSE 中断和额度耗尽时界面状态明确且不会重复扣费。
- 验证用户之间不能访问彼此的文档、分块和对话。
- 在最新版 Chrome 与 Edge 进行 Playwright 端到端测试。
- MVP 验收标准：20 份文本型论文解析成功率不低于 95%；引用跳转准确率不低于 95%；缓存命中时不重复索引；正常网络下首个流式 token 目标小于 3 秒。

## 6. 默认假设

- 首版只支持 Chromium Manifest V3，不承诺 Firefox 和 Safari。
- 面向中文用户，界面和默认翻译目标语言均为简体中文。
- 仅处理文本型 PDF；扫描件和无法可靠提取文本的文档给出明确提示。
- 原始 PDF 不永久上传；云端派生文本只用于检索、摘要和历史恢复。
- 小范围 Beta 不接支付，仅由管理员配置每日额度。
- 不照搬参考产品的品牌、图标、文案和视觉资产，只复用通用交互模式。
- 开发开始前先确认所选翻译服务与 AI 供应商、Supabase 部署区域及数据处理条款满足目标用户所在地区的要求。

翻译路由默认按服务端配置选择供应商。实现时需统一处理签名、QPS、字符数上限、语言代码映射、敏感内容错误和短期结果缓存；供应商密钥仅存在于后端环境变量中。

## 7. 后端部署策略（暂不实施）

当前开发阶段不需要单独租用服务器。插件的翻译和 AI 请求最终必须经过后端，以隔离供应商密钥、执行认证与额度校验、限流和审计；但该后端可先在本地运行。

### 7.1 分阶段部署

1. 开发期：在本地运行 Fastify API（默认 `localhost:8787`），使用本地环境变量，不接入真实用户数据。
2. 小范围 Beta：将 API 部署到托管运行环境，如 Render、Railway、Fly.io、腾讯云 CloudBase、阿里云函数计算或 Supabase Edge Functions；Supabase 负责认证与 PostgreSQL/pgvector。
3. 复杂解析阶段：表格解析、OCR、图片处理等需要 Python 运行时或较长执行时间的任务，再部署为独立容器/worker；按实际吞吐量决定是否租用 VPS 或使用托管容器。

### 7.2 边界与原则

- 扩展端不得保存或调用翻译、AI 供应商密钥。
- 托管 API 足以覆盖翻译、术语解释、摘要和早期文档问答，无需为了 MVP 维护 VPS。
- 部署区域应同时满足目标用户网络条件、供应商可用性及数据处理要求。
- 后端部署前需配置 HTTPS、环境变量、CORS 白名单、请求体上限、日志脱敏、健康检查和错误监控。
- 当前阶段仅保留部署接口与环境变量规范，不产生云服务器或第三方托管费用。

## 8. 后续扩展：图片、表格与伪代码元素

该能力不纳入当前 MVP，放在文本型 PDF 阅读、检索和问答稳定之后实现。目标是识别 PDF 中的图片、表格和伪代码/代码块，并支持定位、复制、翻译和 AI 解释。

### 8.1 统一元素模型

在共享 `packages/contracts` 中增加 `DocumentElement`：

- `text`：文本、页码和可选坐标。
- `image`：图片资源、页码、坐标和可选图注。
- `table`：二维单元格数据、页码、坐标和可选图注。
- `code`：代码或伪代码文本、页码、坐标和可选语言。

所有元素必须保留 `pageNumber`、`bbox` 和原始内容或资产引用，供复制、引用跳转和 AI 上下文使用。

### 8.2 图片识别与操作

- 使用 PDF.js `getOperatorList()` 和页面资源识别嵌入图片，不自行实现 PDF 解码。
- 保存图片所在页码和页面坐标，必要时导出 PNG 或页面区域截图。
- 支持点击图片、复制图片、下载图片、提取图中文字和“解释图片”。
- AI 图片解释统一由后端调用视觉模型，扩展端不保存供应商密钥。

### 8.3 表格识别

按可靠性分层实现：

1. 对文本型表格，使用 PDF.js 文本项坐标、行列间距和边框信息重建单元格。
2. 对复杂表格，在后端复用 PyMuPDF/pdfplumber；不从零编写 PDF 表格解析器。
3. 对无结构或图片表格，渲染表格区域并交给视觉模型，要求严格 JSON 输出，再用 Zod 校验行列结构。

表格应支持复制为 Markdown/CSV、原位置高亮、表格摘要、趋势分析和问答引用。模型输出必须同时保留原始截图，无法校验时标记为低可信，不直接覆盖原始数据。

### 8.4 伪代码与代码块

- 根据等宽字体、缩进、连续短行和 `if/for/while/return` 等特征检测代码块。
- 保存完整文本和缩进，支持一键复制，避免通过普通段落分块破坏格式。
- 可选增加语言识别、逐行解释、代码转换和潜在问题分析。
- AI 请求同时携带代码块前后的少量上下文，避免脱离论文语境。

### 8.5 OCR 兜底

扫描 PDF 或图片内文字不放在浏览器端处理。后续可采用服务端 OCR/视觉模型，并明确标记“这是 OCR 结果”，记录识别置信度；低置信度内容不得生成无来源引用。

### 8.6 推荐开发顺序

1. 图片区域识别、点击定位和复制/下载。
2. 图片 AI 解释与图中文字提取。
3. 代码块检测、保持缩进复制和代码解释。
4. 原生文本表格坐标重建与 Markdown/CSV 导出。
5. 服务端复杂表格解析和视觉模型兜底。
6. OCR、元素级引用和跨页元素高亮。

### 8.7 新增接口方向

- `POST /v1/documents/:id/elements`：上传按页识别出的元素元数据。
- `GET /v1/documents/:id/elements`：按页读取元素和坐标。
- `POST /v1/elements/:id/explain`：解释图片、表格或代码块。
- `POST /v1/elements/:id/export`：导出图片、Markdown、CSV 或纯文本。

该扩展阶段仍遵守原始 PDF 本地保留原则；云端只保存用户明确需要的元素派生数据，并在隐私设置中提供删除入口。
